@using ExamScoringApp.Helpers
@using Microsoft.AspNet.Identity

<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="sidebar-collapse">
        <ul class="nav" id="side-menu">
            <li class="nav-header">
                <div class="dropdown profile-element">
                    <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                        <span class="clear">
                            <span class="block m-t-xs">
                                <strong class="font-bold">@User.Identity.Name</strong>
                            </span> <span class="text-muted text-xs block">More <b class="caret"></b></span>
                        </span>
                    </a>

                    @using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
                    {
                        @Html.AntiForgeryToken()

                    }
                    <ul class="dropdown-menu animated fadeInRight m-t-xs">
                        <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
                        <li><a href="@Url.Action("ChangePassword", "Manage")">Change Password</a></li>

                    </ul>




                </div>
                <div class="logo-element">
                    IN+
                </div>
            </li>
            <li class="@Html.IsSelected(action: "Index",controller:"Home")">
                <a href="@Url.Action("Index", "Home")"><i class="fa fa-dashboard"></i> <span class="nav-label">Dashboard</span> </a>
            </li>


            @* Todo Complete Authorization Part *@

            <li class="@Html.IsSelected(action: "Index", controller: "Exams")">
                <a href="@Url.Action("Index", "Exams")"><i class="fa fa-paperclip"></i> <span class="nav-label">Exams</span></a>
            </li>



            @if (User.IsInRole("Admin"))
            {
                @*<li class="@Html.IsSelected(action: "Index", controller: "Categories")">
                <a href="@Url.Action("Index", "Categories")"><i class="fa fa-star"></i> <span class="nav-label">Categories</span></a>
            </li>*@
                <li class="@Html.IsSelected(action: "Index", controller: "Questions")">
                    <a href="@Url.Action("Index", "Questions")"><i class="fa fa-question"></i> <span class="nav-label">Questions</span></a>
                </li>

                <li class="@Html.IsSelected(action: "Index", controller: "UsersAdmin")">
                    <a href="@Url.Action("Index", "UsersAdmin")"><i class="fa fa-group"></i> <span class="nav-label">Users</span></a>
                </li>

                <li class="@Html.IsSelected(action: "Index", controller: "RolesAdmin")">
                    <a href="@Url.Action("Index", "RolesAdmin")"><i class="fa fa-cogs"></i> <span class="nav-label">Roles</span></a>
                </li>
                @*<li class="@Html.IsSelected(action: "Index", controller: "StudentsPoints")">
                <a href="@Url.Action("Index", "StudentsPoints")"><i class="fa fa-trophy"></i> <span class="nav-label">Students Points</span></a>
            </li>*@
            }





        </ul>
    </div>
</nav>